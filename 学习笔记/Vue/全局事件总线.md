# 全局事件总线

```html
<script>
    new Vue({
        el:"#app",
        render:h=>h(App),
        beforeCreate(){
            Vue.prototype.$bus=this;//安装全局事件总线
        }
        //记得绑定了事件，在使用这个事件的组件销毁时，需要解绑这个事件。
    })
</script>
```

